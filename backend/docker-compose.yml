services:
  postgres:
    image: postgres:16
    container_name: ai_crypto_advisor_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ai_crypto_advisor
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
